plugins{
    id 'java-library'
    id('io.qameta.allure') version '4.0.0'
}

allure {
    report{
        version.set("2.39.0")
    }
    //adapter {   отвечает за появление папки allure-result
        //aspectjWeaver.set(true)
        //frameworks {
            //junit5 { // название фремворка
                //adapterVersion.set("2.33.0")  // версия интеграции фремворка и allure
            //}
        //}
    //}
}

repositories{
    mavenCentral()
}

dependencies{
    testImplementation(
            "com.codeborne:selenide-proxy:7.2.0",
            //"org.junit.jupiter:junit-jupiter:5.9.3",
            "org.junit.jupiter:junit-jupiter:6.0.3",
            "org.slf4j:slf4j-simple:2.0.17",
            "com.github.javafaker:javafaker:1.0.2",
            "io.qameta.allure:allure-selenide:2.34.0"
    )
}

test {
    useJUnitPlatform()
}